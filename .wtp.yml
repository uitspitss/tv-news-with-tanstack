version: "1.0"
defaults:
  base_dir: "../tv-news-worktrees"

hooks:
  post_create:
    - type: copy
      from: ".env"
    - type: copy
      from: ".dev.vars"
    - type: command
      run: bun install
