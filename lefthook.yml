pre-commit:
  commands:
    biome-check:
      glob: "*.{js,jsx,ts,tsx,json}"
      run: bunx --bun biome check --write {staged_files}
    knip:
      run: bun run knip
    typecheck:
      run: bun run type-check
